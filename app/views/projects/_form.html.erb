<%= form_for(project) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= f.collection_select :team_id, Team.all, :id, :name %>
  <%= f.submit %>
<% end %>
